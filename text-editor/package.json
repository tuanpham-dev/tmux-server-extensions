{
  "name": "text-editor",
  "private": true,
  "publisher": "tmux-server",
  "version": "2.1.0",
  "displayName": "Text Editor",
  "description": "A Monaco-based editor tab (the VS Code editor) with syntax highlighting that follows the active theme, IntelliSense for TypeScript/JavaScript/JSON/CSS/HTML, and save-back to disk, for quick edits without a round-trip through nvim.",
  "icon": "./icon.svg",
  "dependencies": {
    "@shikijs/vscode-textmate": "^10.0.2",
    "monaco-editor": "^0.56.0"
  },
  "contributes": {
    "editors": [
      {
        "id": "monaco",
        "label": "Monaco (Text Editor)",
        "capabilities": [
          "file",
          "diff",
          "merge"
        ]
      }
    ],
    "configuration": {
      "title": "Text Editor",
      "properties": {
        "textEditor.minimap": {
          "type": "string",
          "enum": [
            "auto",
            "on",
            "off"
          ],
          "enumItemLabels": [
            "Auto (hidden on touch devices)",
            "Always show",
            "Always hide"
          ],
          "default": "auto",
          "description": "Whether the editor draws the minimap — the code overview down the right edge. Auto shows it on desktop and hides it on phones and tablets, where it only eats width. Diffs and merge conflicts never show one."
        }
      }
    }
  },
  "tmuxServer": {
    "client": "./dist/client.js"
  }
}
