{
  "name": "text-editor",
  "private": true,
  "publisher": "tmux-server",
  "version": "2.0.0",
  "displayName": "Text Editor",
  "description": "A Monaco-based editor tab (the VS Code editor) with syntax highlighting that follows the active theme, IntelliSense for TypeScript/JavaScript/JSON/CSS/HTML, and save-back to disk, for quick edits without a round-trip through nvim.",
  "icon": "./icon.svg",
  "dependencies": {
    "@shikijs/vscode-textmate": "^10.0.2",
    "monaco-editor": "^0.56.0"
  },
  "contributes": {
    "configuration": {
      "title": "Text Editor",
      "properties": {
        "textEditor.extensions": {
          "type": "string",
          "default": "ts,tsx,js,jsx,mjs,cjs,css,py,go,rs,sh,txt,toml",
          "description": "Comma-separated file extensions (no dot) this editor opens for. Excludes json/yml/yaml, md/markdown, html/htm, and csv/tsv by default since those already have a dedicated bundled preview — add one back here if you'd rather edit it as plain text. Reached via the hover Preview icon, the \"Preview\" context-menu item, or Shift+Enter — unless \"Open this editor on a plain FILES click\" is on."
        },
        "textEditor.openOnClick": {
          "type": "boolean",
          "default": false,
          "description": "Open this editor directly on a plain FILES-tree click instead of nvim, for the configured extensions. Takes effect after a page reload."
        }
      }
    }
  },
  "tmuxServer": {
    "client": "./dist/client.js"
  }
}
