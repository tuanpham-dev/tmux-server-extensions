{
  "name": "prompts",
  "private": true,
  "publisher": "tmux-server",
  "version": "1.0.0",
  "displayName": "Prompts",
  "description": "A prompt editor tab for .prompt.md files: \"Prompts: New Prompt\" opens a draft, Refine rewrites it with the AI configured in Settings → AI, and the first save names the file from its content. Existing prompts open via the FILES-tree \"Edit Prompt\" menu item.",
  "icon": "./icon.svg",
  "tmuxServer": {
    "client": "./dist/client.js",
    "server": "./server.js"
  },
  "contributes": {
    "configuration": {
      "title": "Prompts",
      "properties": {
        "prompts.refineInstruction": {
          "type": "string",
          "default": "Rewrite the following prompt so it is clearer, more specific, and better structured for an AI coding agent. Keep the author's intent and every concrete detail; do not invent requirements. Reply with ONLY the rewritten prompt - no preamble, no commentary, no code fences.",
          "description": "The instruction sent to the AI CLI when you press Refine in the prompt editor."
        },
        "prompts.directory": {
          "type": "string",
          "default": "plans/prompts",
          "description": "Where new prompts are saved, relative to the active session's working directory."
        }
      }
    }
  }
}
